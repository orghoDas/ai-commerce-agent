services:
  postgres:
    image: postgres:16
    container_name: ai-commerce-agent-postgres
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
      POSTGRES_DB: ai_commerce_agent
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data

  redis:
    image: redis:7
    container_name: ai-commerce-agent-redis
    ports:
      - "6379:6379"

volumes:
  postgres_data:

